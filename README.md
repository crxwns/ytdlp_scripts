# Missing video Downloader

Compares videos uploaded to a channel with videos downloaded to a folder.

# Usage

Clone the repo and `cd` into it.

```pwsh
uv tool install .

missing --help

missing -c "@XYS" -d PATH/TO/FOLDER
```